"""
agentic_code_editor — FastAPI + Socket.IO + LiteLLM backend for the agentic code editor.

Supports 100+ LLM providers (GLM, Gemini, OpenAI, DeepSeek, Claude, etc.)
via LiteLLM, with a human-in-the-loop workflow: analyze → plan → approve → implement → review.
"""

__version__ = "0.1.0"
